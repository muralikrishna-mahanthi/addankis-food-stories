{%- style -%}
  {%- liquid
    assign heading_font = section.settings.heading_font
    assign text_font = section.settings.text_font
  -%}

  {{ heading_font | font_face: font_display: 'swap' }}
  {{ text_font | font_face: font_display: 'swap' }}

  #AnimationPoints-{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    background: {{ section.settings.bg_color }};
    overflow: hidden;
    font-family: {{ text_font.family }}, {{ text_font.fallback_families }};
  }

  .ap-container {
    max-width: {{ section.settings.container_width }}px;
    margin: 0 auto;
    padding: 0 4rem;
    position: relative;
  }

  .ap-header {
    text-align: center;
    margin-bottom: 10rem;
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.23, 1, 0.32, 1);
  }

  .ap-header.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .ap-heading {
    margin: 0;
    font-size: {{ section.settings.heading_size }}px;
    font-weight: 800; /* Extra bold for the reference look */
    color: {{ section.settings.heading_color }};
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-family: {{ heading_font.family }}, {{ heading_font.fallback_families }};
    max-width: 800px;
    margin: 0 auto;
  }

  .ap-timeline {
    position: relative;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px 0;
  }

  .ap-line-wrapper {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 6px; /* Thicker line from image */
    transform: translateX(-50%);
    background: {{ section.settings.line_base_color }};
    border-radius: 0;
    z-index: 1;
  }

  .ap-line-fill {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 0%;
    background: {{ section.settings.line_active_color }};
    transition: height 0.1s linear;
  }

  .ap-points-list {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 8rem;
    z-index: 2;
  }

  .ap-points-row {
    display: flex;
    justify-content: space-between;
    align-items: flex-start; /* Ensure top-alignment for a parallel look */
    position: relative;
    /* Wrapper handles vertical visibility, children handle horizontal slide */
  }

  .ap-point-item {
    width: 44%;
    position: relative;
    display: flex;
    flex-direction: column;
    opacity: 0;
    transition: transform 1.2s cubic-bezier(0.23, 1, 0.32, 1), opacity 1.2s ease;
  }

  .ap-point-left {
    text-align: center;
    transform: translateX(-100vw); /* Slide from far left */
  }

  .ap-point-right {
    text-align: center;
    transform: translateX(100vw); /* Slide from far right */
  }

  .ap-points-row.is-visible .ap-point-left,
  .ap-points-row.is-visible .ap-point-right {
    opacity: 1;
    transform: translateX(0);
  }

  .ap-point-block {
    padding: 2rem;
    background: transparent; /* No background card */
    box-shadow: none;
    border: none;
    backdrop-filter: none;
  }

  .ap-point-title {
    margin: 0 0 1.5rem 0;
    font-size: 2.4rem;
    font-weight: 700;
    color: {{ section.settings.text_color }};
    font-family: {{ heading_font.family }}, {{ heading_font.fallback_families }};
    line-height: 1.3;
  }

  .ap-point-desc {
    margin: 0;
    font-size: 1.8rem;
    line-height: 1.4;
    color: {{ section.settings.text_color }};
    font-weight: 600; /* Medium-bold look from image */
  }

  @media screen and (max-width: 768px) {
    .ap-container {
      padding: 0 2rem;
    }
    .ap-line-wrapper {
      left: 20px;
      width: 4px;
    }
    .ap-points-row {
      flex-direction: column;
      gap: 4rem;
      padding-left: 40px;
    }
    .ap-point-item {
      width: 100%;
      text-align: left !important;
      transform: translateX(50px) !important; /* Mobile slide from side */
    }
    .ap-heading {
      font-size: 2.8rem;
    }
    .ap-point-desc {
      font-size: 1.6rem;
    }
  }
{%- endstyle -%}

<section id="AnimationPoints-{{ section.id }}" class="ap-section">
  <div class="ap-container">
    <div class="ap-header js-ap-animate">
      <h2 class="ap-heading">{{ section.settings.heading }}</h2>
    </div>

    <div class="ap-timeline">
      <div class="ap-line-wrapper">
        <div class="ap-line-fill js-ap-line-fill"></div>
      </div>

      <div class="ap-points-list">
        {%- for block in section.blocks -%}
          {%- if forloop.index0 % 2 == 0 -%}
            <div class="ap-points-row js-ap-row js-ap-animate">
              
              <!-- Left Point -->
              <div class="ap-point-item ap-point-left" {{ block.shopify_attributes }}>
                <div class="ap-point-block">
                  {%- if block.settings.title != blank -%}
                    <h3 class="ap-point-title">{{ block.settings.title }}</h3>
                  {%- endif -%}
                  {%- if block.settings.description != blank -%}
                    <div class="ap-point-desc">{{ block.settings.description }}</div>
                  {%- endif -%}
                </div>
              </div>

              <!-- Right Point -->
              {%- assign next_index = forloop.index0 | plus: 1 -%}
              {%- assign next_block = section.blocks[next_index] -%}
              {%- if next_block != blank -%}
                <div class="ap-point-item ap-point-right" {{ next_block.shopify_attributes }}>
                  <div class="ap-point-block">
                    {%- if next_block.settings.title != blank -%}
                      <h3 class="ap-point-title">{{ next_block.settings.title }}</h3>
                    {%- endif -%}
                    {%- if next_block.settings.description != blank -%}
                      <div class="ap-point-desc">{{ next_block.settings.description }}</div>
                    {%- endif -%}
                  </div>
                </div>
              {%- endif -%}

            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
</section>



<script>
  function initAnimationPoints() {
    const section = document.querySelector('#AnimationPoints-{{ section.id }}');
    if (!section || section.dataset.initialized) return;
    section.dataset.initialized = "true";

    const animateElements = section.querySelectorAll('.js-ap-animate');
    const rows = section.querySelectorAll('.js-ap-row');
    const lineFill = section.querySelector('.js-ap-line-fill');
    
    // Intersection Observer for fade-in animations
    const observerOptions = {
      threshold: 0.05, // Lower threshold for better visibility
      rootMargin: "0px 0px -40px 0px"
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
        }
      });
    }, observerOptions);

    animateElements.forEach(el => observer.observe(el));

    // Scroll listener for timeline line and dot activation
    function updateTimeline() {
      const rect = section.getBoundingClientRect();
      const viewportHeight = window.innerHeight;
      
      const sectionHeight = section.offsetHeight;
      const sectionTop = rect.top;
      
      // Calculate line progress
      const startTrigger = viewportHeight * 0.8; // Start earlier
      let progress = 0;
      
      if (sectionTop < startTrigger) {
          progress = (startTrigger - sectionTop) / (sectionHeight - viewportHeight * 0.2);
      }
      
      progress = Math.max(0, Math.min(1, progress));
      
      if (lineFill) {
        lineFill.style.height = (progress * 100) + '%';
      }

      rows.forEach(row => {
        const rowRect = row.getBoundingClientRect();
        // Activate dots in the row when it passes 65% of screen height
        if (rowRect.top < viewportHeight * 0.65) {
          row.classList.add('is-active');
        } else {
          row.classList.remove('is-active');
        }
      });
    }

    window.addEventListener('scroll', updateTimeline, { passive: true });
    updateTimeline(); // Initial check
    
    // Safety check: if an element is high up on the page, make it visible immediately
    setTimeout(() => {
        animateElements.forEach(el => {
            const r = el.getBoundingClientRect();
            // Using window.innerHeight here to avoid undefined variable
            if (r.top < window.innerHeight) el.classList.add('is-visible');
        });
    }, 500);
  }

  document.addEventListener('DOMContentLoaded', initAnimationPoints);
  document.addEventListener('shopify:section:load', (e) => {
    if (e.target.id.includes('{{ section.id }}')) {
      initAnimationPoints();
    }
  });
</script>


{% schema %}
{
  "name": "Animation Points",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "The Journey of Excellence"
    },
    {
        "type": "font_picker",
        "id": "heading_font",
        "label": "Heading Font",
        "default": "assistant_n7"
    },
    {
        "type": "font_picker",
        "id": "text_font",
        "label": "Text Font",
        "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading Font Size (px)",
      "min": 24,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 64
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#121212"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#121212"
    },
    {
      "type": "color",
      "id": "line_base_color",
      "label": "Line Base Color",
      "default": "#f0f0f0"
    },
    {
      "type": "color",
      "id": "line_active_color",
      "label": "Line Active Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "block_bg_color",
      "label": "Point Block Background",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "container_width",
      "label": "Container Width (px)",
      "min": 800,
      "max": 1600,
      "step": 100,
      "unit": "px",
      "default": 1400
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 120
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 120
    }
  ],
  "blocks": [
    {
      "type": "point",
      "name": "Point",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Modern Innovation"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Experience the next level of design with our custom animated points section, built for performance and aesthetics.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Animation Points",
      "blocks": [
        { "type": "point", "settings": { "title": "Foundation", "description": "<p>Where it all started, with a vision for excellence.</p>" } },
        { "type": "point", "settings": { "title": "Growth", "description": "<p>Expanding horizons and reaching new milestones.</p>" } },
        { "type": "point", "settings": { "title": "Evolution", "description": "<p>Constant refinement and adaptation to change.</p>" } },
        { "type": "point", "settings": { "title": "Future", "description": "<p>Leading the way into the next era of innovation.</p>" } }
      ]
    }
  ]
}
{% endschema %}

